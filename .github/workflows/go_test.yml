name: Go Test
on:
  pull_request:
    paths: ['go/**/*.go']
jobs:
  go-test:
   runs-on: ubuntu-slim
   steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-go@v6
        with:
          go-version: '1.25'
      - run: go test go/excellent/*.go
